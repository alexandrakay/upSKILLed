import { GeneratePage } from '@/components/generate/GeneratePage';

export const metadata = {
  title: 'Generate — upSKILLed',
  description: 'Generate Claude skill files for any API or CLI tool.',
};

export default function Page() {
  return <GeneratePage />;
}
